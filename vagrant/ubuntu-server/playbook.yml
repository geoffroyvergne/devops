---

- hosts: all
  remote_user: root
  vars:
    partition_type: "lvm"
    disk: "/dev/sda"
    partition_number: 2
  tasks:
    - debug:
        msg: "Ubuntu server provisioning"
  roles:
    - common
    - disk-utils
    #- docker
  become: yes