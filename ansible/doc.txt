
# Ping
ansible all -m ping

# Echo
ansible all -a "/bin/echo hello"

# Playbook

ansible-playbook -i inventories/inventory playbooks/docker.yml
ansible-playbook --limit stretch64 playbooks/docker.yml
ansible-playbook playbooks/docker.yml --list-hosts

# Roles
ansible-galaxy init docker
